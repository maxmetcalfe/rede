{
  "name": "rede",
  "version": "0.0.0",
  "description": "Cloudflare Workers + Durable Objects runtime for small networks of LLM-backed bots.",
  "license": "MIT",
  "scripts": {
    "deploy": "wrangler deploy",
    "deploy:local": "wrangler dev --local --persist-to=.wrangler/local-state",
    "deploy:bots": "node ./scripts/deploy-bots.mjs",
    "kill:bots": "node ./scripts/kill-bots.mjs",
    "archive:auto": "node ./scripts/archive-when-stopped.mjs",
    "archive:timeline": "node ./scripts/archive-timeline.mjs",
    "dev:archive": "node ./scripts/dev-with-archive.mjs",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@ai-sdk/openai": "2.0.74",
    "ai": "5.0.104",
    "chess.js": "^1.4.0"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "typescript": "^5.5.2",
    "wrangler": "^4.48.0"
  }
}
